# Math Adventure

A local browser math rescue game for kids.

Open `index.html` in a browser, or serve the folder with a simple local server:

```sh
python3 -m http.server 4173
```

Then visit `http://127.0.0.1:4173/`.

